import React, { Fragment } from 'react';
import { PageChooser, NavBar } from '../../components';

const UserHome = () => {
	return (
		<Fragment>
			<NavBar />
			<PageChooser />
		</Fragment>
	);
};

export default UserHome;
